Alert is an inline callout — a tinted surface with a colored accent bar.

```jsx
<Alert variant="success" title="Banked">+$42,000 added to market value.</Alert>
```

Variants: `info` · `success` · `warning` · `danger`.
