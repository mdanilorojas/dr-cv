Input is the standard text field — accent focus ring, optional label and help text, and a `search` variant with a leading magnifier.

```jsx
<Input label="Objective" help="Outcome-first, always." defaultValue="$250,000 / year" />
<Input search placeholder="Search skills…" />
```
