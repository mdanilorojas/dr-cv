Select is a native `<select>` styled to match Input — same field chrome, custom caret.

```jsx
<Select label="Skill cluster">
  <option>Systems thinking</option>
  <option>Prototyping</option>
</Select>
```
