Tabs is the underline tab bar — accent indicator under the active tab.

```jsx
<Tabs value={tab} onChange={setTab}
  items={[{id:'overview',label:'Overview'},{id:'gaps',label:'Gaps'}]} />
```

For 2–3 mutually exclusive options inside a toolbar, prefer Segmented.
